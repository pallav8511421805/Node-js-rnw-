<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>mongo</title>
    <link rel="stylesheet" type="text/css" href="style.css">
    <link rel="stylesheet" type="text/css" href="all.css">
</head>
<body>
    <section>
        <div class="container">
            <div class="row ">
                <div class="box">
                    <div class="w100">
                      
                      <div class="row">
                        <a href="/create" class="atag">create user</a>
                      </div>

                        <table>
                          <thead>
                            <tr>
                                <th>No.</th>
                                <th>Name</th>
                                <th>Age</th>
                                <th>Status</th>
                                <th>Edit</th>
                                <th>Delete</th>
                            </tr>
                          </thead>
                          <tbody>
                            <% if(data){ let i=1; for(user of data) { %>
                              <tr>
                                <td><%= i %></td>
                                <td><%= user.name %></td>
                                <td><%= user.age %></td>
                                <td><%= user.status %></td>
                                <td><div class="textcenter"><a href="<%= `/edit?id=${user.id}` %>"><div class="btnaction"><i class="fa-solid fa-pencil"></i></div></a></div></td>
                                <td><div class="textcenter"><a href="<%= `/delete?id=${user.id}` %>"><div class="btnaction"><i class="fa-solid fa-trash-can"></i></div></a></div></td>
                              </tr>
                                <% i++; } } %>
                          </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </section>
</body>
</html>